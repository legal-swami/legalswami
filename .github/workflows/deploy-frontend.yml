name: Build Pipeline

on:
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Show ACTUAL folder structure
        run: |
          echo "📁 ACTUAL Project Structure:"
          echo "============================="
          ls -la
          echo ""
          echo "📂 Looking for 'backend' folder (not legalswami-backend):"
          ls -la backend/ || echo "Trying different names..."
          echo ""
          echo "✅ Checking Java files:"
          find . -name "*.java" | head -5
